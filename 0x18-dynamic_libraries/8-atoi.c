#include "main.h"

/**
 * _atoi - Entry point
 * @s: string to be converted
 *
 * Description: Converts a string to an int.
 *
 * Return: Always 0.
 */
int _atoi(char *s)
{
	if (*s)
	{
		return (0);
	}
	return (0);
}
