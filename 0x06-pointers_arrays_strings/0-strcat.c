#include "main.h"
#include <string.h>

/**
 * _strcat - Entry point.
 * @src: source
 * @dest: destination
 *
 * Description: Concatenates two strings.
 *
 * Return: Alwats 0
 */
char *_strcat(char *dest, char *src)
{
	return (strcat(src, dest));
}
