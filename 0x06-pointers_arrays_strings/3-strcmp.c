#include "main.h"
#include <string.h>

/**
 * _strcpm - Entry point
 * @s1: string one
 * @s2: string two
 *
 * Description: Compares two strings using strcmp
 *
 * Return: Always 0
 */
int _strcmp(char *s1, char *s2)
{
	return (strcmp(s1, s2));
}
