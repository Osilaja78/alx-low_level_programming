#include "main.h"

/**
 * mul - Entry point
 * @a: first variable
 * @b: second variable
 *
 * Description: 'This program prints the multiple of two numbers'
 *
 * Return: @c - the multiple of both numbers
 */
int mul(int a, int b)
{
	int c;

	c = a * b;
	return (c);
}
