#include "main.h"
#include <string.h>

/**
 * _strlen - prints lenght of a string
 * @s: variable to be checked
 *
 * Descriptipn: prints length of string
 *
 * Return: Always 0
 */
int _strlen(int *s)
{
	int b = strlen(*s);

	return (b)
}
