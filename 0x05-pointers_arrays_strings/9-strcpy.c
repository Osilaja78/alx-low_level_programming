#include "main.h"
#include <string.h>

/**
 * _strcpy - Entry point
 * @dest: destination
 * @src: source
 *
 * Description: copies string from source to deatination
 *
 * Return: Always 0
 */
char *_strcpy(char *dest, char *src)
{
	return (strcpy(dest, src));
}
