#include "main.h"
#include <ctype.h>

/**
 * _abs - Entry point
 *
 * Description: 'This program prints the absolute of a number'
 *
 * Return: Always 0 (success)
 */
int _abs(int)
{
	_putchar(abs(int));
	return (0);
}
